# 📚 Lo que aprendí

- **Variables** → permiten almacenar datos y reutilizarlos en operaciones o mensajes.  
- **input()** → recibe datos como cadenas y se pueden convertir con `float()` o `int()` para cálculos.  
- **print()** → muestra resultados en pantalla y combina texto con variables.  
- **Formateo de cadenas** → se puede hacer con `.format()` o con **f-strings**.  
- **round()** → redondea números al entero más cercano o a un número específico de decimales.  
- **Operadores matemáticos** → `+`, `-`, `*`, `/`, `//`, `%`, `**` permiten cálculos básicos y avanzados.  

---

# 💡 Mini proyecto: Cálculo de comisiones

## 📖 Descripción
El proyecto consistió en crear un programa que ayude a los vendedores a calcular su comisión del **13%** sobre las ventas del mes.  

## ⚙️ Explicación
- Se pidió al usuario su nombre y el total de ingresos con `input()`.  
- Los ingresos se convirtieron a `float` para poder operar con ellos.  
- Se calculó el **13%** de los ingresos con una operación matemática.  
- Se usó `round()` para mostrar el resultado con dos decimales.  
- Se imprimió un mensaje final usando **f-strings**, incluyendo el nombre y la comisión calculada.  

---

# ✅ Solución
El reto pedía mostrar el nombre del vendedor junto con el monto de su comisión.  
La solución fue usar **variables, conversión de tipos, operaciones matemáticas y formateo de cadenas** para obtener y mostrar el resultado de manera clara.
